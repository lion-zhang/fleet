__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/
.pytest_cache/

# Runtime state -- lives in the OS config/state dirs, never in the repo.
# Listed defensively in case it is ever copied here for debugging.
inventory.yaml
config.yaml
secrets.age
*.db
*.db-wal
*.db-shm

# Local data / scratch -- never pushed.
data/
.remember/
